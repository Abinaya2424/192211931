#include<stdio.h>
int main()
{
	int i=1,number;
	int fact=1;
	
	printf("enter an integer:");
	scanf("%d",&number);
	
	for(i=1;i<=number;++i)
	fact=fact*i;
	
	printf("factorial of %d is:%d",number,fact);
	return 0;
}
