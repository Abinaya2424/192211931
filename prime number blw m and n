#include<stdio.h>
#include<math.h>
void main() {
 int i,m,n,d;

 printf("Enter the Range Between m,n ");
 scanf("%d%d", &m,&n);
 for (i= m; i <= n; i++) {
   for(d = 2; d < i; d++) {
    if (i % d == 0)
      break;
   }
   if (d == i)
     printf("%d ", i);
 }
  
}
