#include <stdio.h>
int main() 
{
    char c;
    int vowel,consonant;
    printf("Enter an alphabet: ");
    scanf("%c", &c);
        if (vowel)
        printf("%c is a vowel.", c);
    else
        printf("%c is a consonant.", c);
    return 0;
}
